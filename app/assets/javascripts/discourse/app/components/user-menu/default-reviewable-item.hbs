<li class={{if @item.pending "pending"}}>
  {{#link-to "review.show" @item.id}}
    {{d-icon this.icon}}
    <div>
      <span>{{this.actor}}</span>
      <span>{{this.description}}</span>
    </div>
  {{/link-to}}
</li>
